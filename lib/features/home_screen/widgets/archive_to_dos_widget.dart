import 'package:flutter/material.dart';

class ArchiveToDosWidget extends StatelessWidget {
  const ArchiveToDosWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('archive to-dos'),
    );
  }
}
