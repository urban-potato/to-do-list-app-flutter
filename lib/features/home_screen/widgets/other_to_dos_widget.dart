import 'package:flutter/material.dart';

class OtherToDosWidget extends StatelessWidget {
  const OtherToDosWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('other to-dos'),
    );
  }
}
